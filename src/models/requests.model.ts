import { DataTypes } from "sequelize";
const RequestsModel = {
    id: {
        type: DataTypes.INTEGER,
        autoIncrement: true,
        primaryKey: true,
    },

    /* AGREGAR LOS QUE FALTAN */

};    

export {RequestsModel}; 